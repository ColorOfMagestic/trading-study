# На GitHub Pages публикуются ТОЛЬКО HTML-страницы.
# Логика — «белый список»: сначала игнорируем всё, потом разрешаем нужное.
# Всё, что не перечислено ниже (CLAUDE.md, study/course-log.md, method-rules.md,
# 06-crypto-plan.md, шаблоны, .claude/, .vscode/) остаётся только на диске.

*

# Разрешаем заходить внутрь папок
!*/

# Служебные файлы публикации
!.gitignore
!.nojekyll

# Сами страницы
!index.html
!study/*.html
!journal/*.html
